This folder contains the base classes to implement th new hierarchical components.

It provides a base class for common functionality, and two component, one port and two ports components, 
that allow the impementation of sources and sinks, and respectively filters and codecs. 

These base components are included in the omil library, with the core functionality,
that each final component must include.
